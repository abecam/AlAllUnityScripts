using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveClouds : MonoBehaviour
{
    public GameObject cloudsWall;
    public GameObject cloud1;
    public GameObject cloud2;
    public GameObject clouds1;
    public GameObject clouds2;

    // Start is called before the first frame update
    void Start()
    {
        // Create a null object for each type of cloud to scroll them up

        // CloudWalls back to front, repeatedly on y

        // Clouds1 (randomized on x,y,z)

        // Clouds2 (randomized on x,y,z)

        // Clouds-s1 (randomized on x,y,z)

        // Clouds-s2 (randomized on x,y,z)
    }

    // Move everything up with variable speed
    void Update()
    {
        
    }
}
